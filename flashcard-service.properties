
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.properties.hibernate.show-sql=true
spring.jpa.properties.hibernate.format-sql=true
spring.jpa.properties.hibernate.ddl-aute=update

#eureka.client.register-with-eureka=true
#eureka.client.service-url.defaultZone=http://localhost:8761

#^^ not strictly necessary, as a Boot app with spring-cloud-netflix-eureka-client in the classpath
# will automatically attempt to send heartbeats to a Eureka server at localhost:8761
